@model List<(string NombreEspecie, int Cantidad)>

@{
    ViewData["Title"] = "Cantidad por Especie";
}

<h1>Cantidad de Mascotas por Especie</h1>

@if (Model.Count > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th>Especie</th>
                <th>Cantidad</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.NombreEspecie</td>
                    <td>@item.Cantidad</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="alert alert-info">No hay mascotas registradas.</div>
}

<p>
    <a asp-action="Index" class="btn">Volver al Listado</a>
</p>

